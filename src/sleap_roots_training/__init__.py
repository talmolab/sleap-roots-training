"""Config-driven training and evaluation of SLEAP root models.

This package wraps the `sleap-nn` backend in a reproducible, OmegaConf-driven
pipeline for training and evaluating generalist and per-crop root models, with
Weights & Biases experiment tracking. It replaces the notebook-based
`eberrigan/sleap-roots-training` workflow.
"""

__version__ = "0.0.1a0"
